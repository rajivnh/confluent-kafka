server:
  port: 5000

spring:
  kafka:
    bootstrap-servers:
      - 'localhost:9092'
    producer:
      value-serializer:
        org.springframework.kafka.support.serializer.JsonSerializer
        
